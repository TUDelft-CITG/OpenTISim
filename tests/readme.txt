to run the tests do

pip install pytest
pip install pytest-cov

Proposed tests:
- for a given demand calculate number of terminal elements
- for a given demand calculate total storage
- test proposed CAPEX and OPEX for given input
- test kendall functioning of jetty module
- test working of terminal supplychain elements
- test if transitions around a demand discontinuity are working properly
